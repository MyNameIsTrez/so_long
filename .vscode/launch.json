{
	"version": "0.2.0",
	"configurations": [{
		"name": "so_long",
		"type": "cppdbg", // Can't use lldb since it doesn't support expression based breakpoints.
		// "type": "lldb", // Can't use lldb since it doesn't support expression based breakpoints.
		"request": "launch",
		"program": "${workspaceFolder}/so_long",
		"args": [],
		// "stopAtEntry": false,
		"cwd": "${workspaceFolder}",
		// "reverseDebugging": true,
		// "environment": [],
		// "externalConsole": false,
		"MIMode": "lldb",
		"preLaunchTask": "Build so_long",
		"internalConsoleOptions": "neverOpen"
	}]
}
