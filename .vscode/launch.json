{
	"version": "0.2.0",
	"configurations": [
	{
		"name": "so_long",

		// Prints to the debug console instead of the terminal
		// Can't see statics in debugger
		"type": "cppdbg",
		"MIMode": "lldb",

		// Doesn't support expression-based breakpoints
		// Casting in watch window will give "invalid syntax"
		// "type": "lldb",

		"request": "launch",
		"program": "${workspaceFolder}/so_long",
		"args": [
			// "maps/two_players.ber",
			// "maps/two_player_1_characters.ber",
			// "maps/two_players_twice.ber",
			"maps/monster.ber",
			// "1"

			// TODO: Make sure this nonexistent file prints a proper error!
			// "foo.ber",

			// "maps/1_MB.ber",
			// "1",

			// "maps/100_kB.ber",
			// "1"
		],
		// "stopAtEntry": false,
		"cwd": "${workspaceFolder}",
		// "reverseDebugging": true,
		// "environment": [],
		// "externalConsole": false,
		"preLaunchTask": "Build so_long",
		"internalConsoleOptions": "neverOpen"
	}]
}
