{
	"version": "0.2.0",
	"configurations": [{
		"name": "so_long",
		// "type": "cppdbg", // Prints to the debug console instead of the terminal.
		"type": "lldb", // Doesn't support expression-based breakpoints.
		"request": "launch",
		"program": "${workspaceFolder}/so_long",
		"args": [
			"maps/example_1.ber",
			"asdi"
		],
		// "stopAtEntry": false,
		"cwd": "${workspaceFolder}",
		// "reverseDebugging": true,
		// "environment": [],
		// "externalConsole": false,
		// "MIMode": "lldb",
		"preLaunchTask": "Build so_long",
		"internalConsoleOptions": "neverOpen"
	},
	{
		"name": "so_long testing",
		// "type": "cppdbg", // Prints to the debug console instead of the terminal.
		"type": "lldb", // Doesn't support expression-based breakpoints.
		"request": "launch",
		"program": "${workspaceFolder}/so_long",
		"args": [
			"map_tests/two_players.ber",
			"asdi"
		],
		// "stopAtEntry": false,
		"cwd": "${workspaceFolder}",
		// "reverseDebugging": true,
		// "environment": [],
		// "externalConsole": false,
		// "MIMode": "lldb",
		"preLaunchTask": "Build so_long",
		"internalConsoleOptions": "neverOpen"
	}]
}
